from django.urls import path

#from .views import PositionApiView
from hagelandse101 import views

urlpatterns = [
    #path('positions/', PositionApiView.as_view()),
    #path('positions/<int:id>', PositionApiView.as_view())
    
]